#!/usr/bin/node

/* Require the 'add' function from the '13-add.js' module. */
const add = require('./13-add').add;

/* Use the 'add' function to calculate and print the sum of two integers. */
console.log(add(3, 5));
